<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Thread</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
    <link rel="stylesheet" href="css/style.css">
</head>
<body>
    <h1>Messages:</h1>
    <ul class="messages">
        <!-- loop to list the messages located inside of a thread that are stored in info.messages -->
        <h2><%= String(info.messages[0].message) %></h2>
    <% if (info.messages[0].message != false) { %>
    <% for(let i=0; i < info.messages.length; i++) {%>
        <li class="message">
            <h2 class = "userName"><%= info.messages[i].userName %></h2>:
            <span><%= info.messages[i].message %></span>
            <span><%= info.messages[i].likes %></span>
            <span class='fa fa-thumbs-up'></span>
            <span class='fa fa-trash'></span>
        </li>
    <% } %>
    <% } else{ %>
        <li>no messages</li>
    <% } %>
    </ul>

    <h2>add A message:</h2>

    <form action="/threads/addMessage" method="POST">
        <input type = "hidden" name="threadName" value="<%= info.threadName %>">
        <input type="text" placeholder="user name" name="userName">
        <input type="text" placeholder="message" name="message">
        <input type="submit">
    </form>
    

    <script src='js/main.js'></script>
</body>
</html>
